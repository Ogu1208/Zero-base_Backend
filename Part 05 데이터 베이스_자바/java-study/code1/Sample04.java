package code1;

public class Sample04 {
  public void test() {

    int a = 10;
    int b = 20;

    // 이항 연산자 (피연산자:2개)
    // 단항연산자 (피연산자:1개)
    // 삼항연산자 (피연산자:3개)

    // a+b
    // +, -, *, /, %

    // >, >=, <, <=, ==, instanceof
    // &, ^, |, &&, ||
    // (a == b) ? (c) : (d)
  }
}
